# Tuples are immutable (an immutable object is an object whose state cannot be modified after it is created.)
# Good for data that is never going to change
coordinates = (32.04950, 91.20394)
print(coordinates[1])
